#include "User_Types.h"
#include "User_Types.h"
int LoadSettings();
int SaveSettings();
int SettingsMenu(HINSTANCE hInstance, HWND HandleWinMain);
int LoadLastFiles();
int SaveLastFiles(char vNewFilepath[vFilePathLenght]);